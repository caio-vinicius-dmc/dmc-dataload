<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Redirecionar todas as requisições para a pasta public/
    RewriteCond %{REQUEST_URI} !^/DMC-DATALOAD/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
